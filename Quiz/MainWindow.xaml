<Window x:Class="Quiz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quiz"
        xmlns:viewModel="clr-namespace:Quiz.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1080"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        >
    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>
    
    <Border Background="#E5E4E2" CornerRadius="24">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Text="Quiz App" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#36454F" FontSize="24" FontWeight="SemiBold"/>

            <StackPanel Grid.Row="1">
                <RadioButton IsChecked="True" Command="{Binding GeneratorViewCommand}" Content="Generator" Style="{StaticResource MenuButtonTheme}"/>
                <RadioButton Command="{Binding QuizViewCommand}"  Content="Quizzes" Style="{StaticResource MenuButtonTheme}"/>
            </StackPanel>

            <Border Padding="10" Background="#C0C0C0" CornerRadius="16" Margin="10" Grid.RowSpan="2" Grid.Column="1">
                <Border.Clip>
                    <RectangleGeometry RadiusX="16" RadiusY="16" Rect="0,0,840,600"/>
                </Border.Clip>
                
                <ContentControl 
                    Content="{Binding CurrentView}"
                />
            </Border>
        </Grid>
    </Border>
</Window>
