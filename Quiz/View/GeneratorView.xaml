<UserControl x:Class="Quiz.View.GeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quiz.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Border CornerRadius="12" Background="#E5E4E2" Padding="10,5,10,5" HorizontalAlignment="Left" VerticalAlignment="Top">
            <TextBlock Text="Creating a new quiz" FontSize="12" Foreground="#36454F"/>
        </Border>

        <Border Grid.Row="1" CornerRadius="12" VerticalAlignment="Stretch" Margin="0,20,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="Quiz title" Style="{StaticResource InputLabel}"/>
                    <TextBox Style="{StaticResource FormInput}" HorizontalAlignment="Left" Width="250" Text="Title..."></TextBox>
                </StackPanel>
                <StackPanel Grid.Row="1" Margin="0,10,0,0">
                    <TextBlock Text="Questions list" Style="{StaticResource InputLabel}"/>
                    <ListView VerticalAlignment="Stretch" />
                    <Button Content="Add question" Style="{StaticResource FormButtonTheme}" Margin="0,10,0,0"/>
                </StackPanel>
                <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Save quiz" Style="{StaticResource FormButtonTheme}" Margin="0,0,10,0"/>
                    <Button Content="Edit Questions" Style="{StaticResource FormButtonTheme}"/>
                </StackPanel>
                
            </Grid>
            
        </Border>
        
        
    </Grid>
</UserControl>
